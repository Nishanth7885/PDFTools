export const metadata = { title: 'Text to PDF Converter Free — Create PDF from Text | SafeFileConverter', description: 'Convert text and notes into a formatted PDF document for free. Supports headings, bullet points. No signup, instant download.' };
export default function Layout({ children }) { return children; }
