export const metadata = { title: 'Split PDF Online Free — Extract Pages from PDF | SafeFileConverter', description: 'Split PDF files and extract specific pages for free. Enter page ranges like 1-3, 5, 8-10. No signup required. Files deleted immediately after processing.' };
export default function Layout({ children }) { return children; }
